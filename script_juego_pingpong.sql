CREATE TABLE JUEGO_PINGPONG (
    ID_RESULTADO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    JUGADOR VARCHAR2(50),
    PUNTAJE_JUGADOR NUMBER,
    PUNTAJE_CPU NUMBER,
    FECHA_REGISTRO DATE DEFAULT SYSDATE
);

COMMIT;
